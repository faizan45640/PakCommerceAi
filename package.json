{
  "name": "pakcommerce-ai",
  "version": "0.1.0",
  "private": true,
  "description": "PakCommerce AI — Final Year Project monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run build",
    "ci:ml": "cd apps/ml && python3 -m pip install -r requirements-dev.txt && ruff check . && ruff format --check . && pytest tests/ -q"
  },
  "engines": {
    "node": ">=20"
  }
}
